class AppConstants {
  const AppConstants._();

  static const String appName = 'To Do App';
  static const Duration fastAnimation = Duration(milliseconds: 220);
  static const Duration mediumAnimation = Duration(milliseconds: 360);
  static const Duration slowAnimation = Duration(milliseconds: 520);
}
