name: to_do_app
description: Premium Flutter to-do application with Firebase REST integration.
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: "^3.11.1"
dependencies:
  connectivity_plus: ^7.0.0
  cupertino_icons: ^1.0.8
  dio: ^5.9.2
  equatable: ^2.0.8
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  flutter_dotenv: ^6.0.0
  flutter_secure_storage: ^10.0.0
  get_it: ^9.2.1
  google_fonts: ^8.0.2
  intl: ^0.20.2
  pretty_dio_logger: ^1.4.0
dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
  assets:
    - .env
